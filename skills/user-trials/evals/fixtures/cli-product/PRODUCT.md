# deployctl

`deployctl` is used by application developers preparing occasional releases and
release operators coordinating several deployments per day. Production deploys
require a separate approval and are out of scope for this fixture.

Run it with `node deployctl.mjs`.
